[build]
# Dockerfile 빌더 사용 (Chrome/Selenium 지원)
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
numReplicas = 1
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
# 기본 health check (DB/Redis 제외)
healthcheckPath = "/actuator/health"
healthcheckTimeout = 300
